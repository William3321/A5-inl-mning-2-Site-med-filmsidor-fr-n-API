<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
</head>
<body>
    <h1>An error occurred</h1>
    <p><%= message %></p>

    <% if (consoleError) { %>
        <p>Console Error: <%= consoleError %></p>
    <% } %>

    <% if (message === 'Movie not found') { %>
        <p>The requested movie was not found. <a href="/">Go back to the home page</a>.</p>
    <% } %>
</body>
</html>